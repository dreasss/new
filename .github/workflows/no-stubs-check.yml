name: no-stubs-check

on:
  push:
  pull_request:

jobs:
  no-stubs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run no stubs check
        run: ./scripts/no_stubs_check.sh
